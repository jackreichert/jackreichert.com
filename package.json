{
  "name": "jackreichert.com",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static build of jackreichert.com, synced from WordPress",
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "build": "eleventy && pagefind --site _site",
    "dev": "eleventy --serve"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "turndown": "^7.2.0",
    "turndown-plugin-gfm": "^1.0.2"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
